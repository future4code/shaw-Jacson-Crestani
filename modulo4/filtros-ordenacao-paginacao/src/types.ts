export type user = {
   id: string
   name: string
   email: string
   type: string
}

export type recipe = {
   id: string
   name: string
   email: string
   password: string
   recipes?: recipe[]
}